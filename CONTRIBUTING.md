# 開発者ガイド

このプロジェクトへの貢献に関心を持っていただきありがとうございます。

## 開発フロー

1. **Issueの作成**: バグ報告や機能追加の提案はIssueを作成してください。
2. **Branchの作成**: `main` ブランチから新しいブランチを作成してください。
   - 命名規則: `feature/feature-name` または `fix/bug-name`
3. **実装**: コードを実装します。
4. **テスト**: 変更に対するテストを追加・実行してください。
   ```bash
   npm test
   ```
5. **Pull Request**: 実装が完了したらPull Requestを作成してください。

## コーディング規約

- **TypeScript**: 型定義を適切に行ってください。`any` の使用は極力避けてください。
- **React**: 関数コンポーネントとHooksを使用してください。
- **Lint/Format**: コミット前にLintを実行してください。
  ```bash
  npm run lint
  ```

## ディレクトリ構造

`README.md` のプロジェクト構成セクションを参照してください。

## 重要な仕様ドキュメント

### 共用部按分の仕様
消防法用途別面積計算における共用部按分の仕様は、`COMMON_AREA_SPEC.md` に詳細に記載されています。
**共用部に関する実装を行う際は必ずこのドキュメントを参照してください。**

重要なポイント：
- 建物共用部は全用途コードに按分されるが、**その階にのみ加算される**
- グループ共用部はグループ内の用途コードに按分されるが、**その階にのみ加算される**
- 按分により仮想用途が自動的に作成される

詳細な例と計算式は `COMMON_AREA_SPEC.md` を参照してください。

## AI エージェントによる開発

このプロジェクトは AI エージェント (`.kiro/` 以下の設定) を使用した開発フローを含んでいます。
詳細は `AGENTS.md` を参照してください。
